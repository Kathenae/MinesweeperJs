

// Given a Minefield with the following properties:
// +getWidth() [Integer]
// +getHeight() [Integer]
// +getCell(x, y) [Object typeof Cell]
// +touchCell(x, y) [Array containing revelead cells]
// +isSolved()
// +hasExploded()

// Assuming each cell has following properties:
// +getX() [Integer]
// +getY() [Integer]
// +isRevealed() [Boolean]
// +getAdjecentMinesCount() [Integer]

// Create a function that Solves the minefield 
// and returns a list of positions that
// need to be touched in order to solve it
export default function solve(minefield){
  
}